'use strict';

var chunkProcessor = require('../ugly-chunk-processor');
require('should');

describe('Chunk Processor', function(){

  it('should return -1 when the value is not present');

  it('exists', function(done){
    debugger;
    chunkProcessor.should.be.ok;
    done();
  });

});